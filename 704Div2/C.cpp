#include <bits/stdc++.h>

using namespace std;
 

int main()
{  
    int n, m;
    cin >> n >> m;
    string s, t;
    cin >> s >> t; 

    unordered_map<char,vector<int> > um;
    for(int i = 0; i < n; i++)
        um[s[i]].push_back(i);
    
    int maxLen = INT_MIN;
    int currLen = 0;

    int idx = 0;

    for(int i = 0; i < m; i++)
    {
        for()
    }

   
} 

